{
  "version": 2,
  "framework": "Create-React-App",
  "buildCommand": "npm run build --prefix frontend && npm install -g serve && serve -s build",
  "installCommand": "npm install --prefix frontend",
  "devCommand": "npm start --prefix frontend",
  "outputDirectory": "frontend/build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "VITE_API_URL": "https://nfl-predict-ecf5a5bd34fe.herokuapp.com",
    "REACT_APP_API_URL": "https://nfl-predict-ecf5a5bd34fe.herokuapp.com"
  }
}
