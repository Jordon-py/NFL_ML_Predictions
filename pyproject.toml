# Project metadata used by Python tooling and Vercel's Python runtime selector.
# Data shape: TOML [project] metadata plus deployment-compatible Python bounds.
# Syntax: PEP 621 keys; no functions live here, so there are no function line numbers.
# Possible bug: widening requires-python can make Vercel choose a newer runtime before
# compiled ML/data wheels exist. Extend by moving runtime deps into dependencies or
# adding optional dependency groups for dev/test tooling.
[project]
name = "nfl-ml-predictions"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.12.0"
dependencies = []
