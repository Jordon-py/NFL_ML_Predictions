# CI Repair Verification - 2026-05-22

This note intentionally triggers the normal `main` CI/deploy workflows after the repair commit `4ef25a8135fd1b860abf3e3cfbb7bfc4d5cd23b2`.

Repair scope:

- Move the in-memory history metrics endpoint to `/history/summary/memory` so `/history/summary` remains request/user scoped.
- Harden SQLite history summaries to use joined final score rows when prediction rows have not been denormalized yet.
- Align `backend/data/datasets/latest_dataset.json` and `backend/models/metadata.json` to the packaged deployed dataset `backend/data/datasets/game_features_20260123.csv` with SHA256 `b62a0845dabf13c9b7a53ce7118b9b8e3d72c0a69c748fcb2d4ea30d879690f9`.

Verification target:

- GitHub Actions CI backend tests should pass.
- Heroku deploy should complete and `/status/models` plus `/predict` should continue reporting classifier-backed predictions.
